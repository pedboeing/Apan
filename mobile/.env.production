# API de produção (Prisma Compute), pronta pra qualquer pessoa acessar de
# qualquer rede — use isso quando for soltar o app pra alguém de fora testar.
# Pra usar: copie o valor abaixo pra EXPO_PUBLIC_API_URL no seu .env, rode
# `npx expo start` de novo e compartilhe o QR code (funciona em qualquer
# rede Wi-Fi/dados móveis, não precisa mais estar na mesma rede que você).
EXPO_PUBLIC_API_URL=https://rqhlujs2xpigf5229hbzdwds.ewr.prisma.build
